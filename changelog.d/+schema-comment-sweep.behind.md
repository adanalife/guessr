The deploy check's advice when a tier's database is missing a table now names the migrations the game actually ships and the command that applies them, instead of a file and a task that no longer exist.
