**`rebuild.py` and the clips worker now read a clip name the same way.** `parse_image` accepted any run of trailing digits as a millisecond where `functions/clips/[[path]].js` requires at least six, so a legacy slug ending in `-123` was a moment to one and no moment to the other. The disagreement failed safe — `check` caught the resulting slug mismatch and refused — but it refused with the wrong diagnosis, skipping the moment-less branch written for exactly those pre-guessr#81 rounds and reporting that the key and the round disagree instead of that the moment is recorded nowhere.
