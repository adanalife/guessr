Building a new set of rounds is faster to get right: the picking can be tried out on its own without cutting any footage, and a round whose clip comes out empty is re-cut rather than sinking the whole batch.
