Release-please now runs serially, can be re-run by hand, and is recognized by branch prefix — two overlapping runs can no longer half-update the release PR, a stale release branch can be rebased off current main without waiting for a releasable commit, and the changelog gate exempts any release-please branch rather than only the single-package one.
