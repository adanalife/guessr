Release-please now runs serially and can be re-run by hand — two overlapping runs can no longer half-update the release PR, and a stale release branch can be rebased off current main without waiting for a releasable commit.
