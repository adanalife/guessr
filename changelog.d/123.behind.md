Production's schedule can now keep itself filled: a top-up run reads how far ahead the game is scheduled, generates only what is missing, and never places a new day inside the review window — so fresh rounds are always visible on the admin page for a few days before anyone can play them.
