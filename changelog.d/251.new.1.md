A browser error reported from the web game now carries a Session Replay of the minute leading up to it, with every piece of text masked and every image blocked, so a bug report shows what the player did to reach it. Sessions that never error send nothing.
