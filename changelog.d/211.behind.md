The integration run holds every route to its HTTP contract, the admin surface and the clip endpoint included, against a throwaway local database and bucket, so the server side can be rewritten against a suite that does not care what language it is in.
