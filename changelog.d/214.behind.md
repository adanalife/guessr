Applying a database migration now always ends by listing what is still unapplied, including when the apply dies partway, so a half-migrated database is visible the moment it happens rather than when something reads a missing column.
