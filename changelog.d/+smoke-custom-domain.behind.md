The checks that run after every deploy now try the daily review page at the game's own web address as well as its internal one, which is the only place a mistyped sign-in setting shows itself — the sort that quietly shut that page for a day.
