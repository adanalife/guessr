The alias wordlists live in one file, `web/alias.json`, which the page, the scoring endpoint and the Python server all load. The page now needs a browser that supports JSON module imports (Safari 17.2, Firefox 138, Chrome 123 or later).
